# داقش — قواعد المشروع

لعبة ورق/مداقشة خليجية. كل شي في `index.html` (ملف واحد). لايف: https://waelsaballyl.github.io/dagesh/
الأوضاع: غرف أونلاين (MQTT، المضيف هو الحكم)، ضد البوتات.

## النشر
`python bump.py` (يحدّث `const BUILD` فتنعش الصفحات نفسها) ← commit ← push.

## الاختبار قبل أي push
`python -m http.server PORT` + Browser pane على مقاسات الجوال (375×667، 430×932) واللاب (1366×657، 1280×640، 1903×836). أوقف السيرفر بالـ PID بعدها.

## اللغة
الرد بالعربي. الواجهة كلها عربي — لا تدخل كلمات غامضة، وخلّ النصوص قصيرة والتفاصيل خلف توغل "💡 اشرح لي".

## الذوق (لا تناقش فيها)
- **الصوت:** هادئ ومحترم، لا شي روبوتي أو غريب. الانفجار مطلوب، صوت الصاروخ لا. أي كليب جديد: من Pixabay، مستواه ~-24 dB (max 50ms RMS)، ويتسجّل في `sfx/CREDITS.txt`. الضيوف يسمعون عبر فروقات الحالة في `applyState` (باقي المنطق للمضيف فقط).
- **البصري:** مودرن، لا يشبه 2010. الطراطيع canvas متوهّجة.
- **الأداء:** قِس قبل ما تحسّن. الحل اللي نفع (26ms ← 5ms): `clearRect` بدل تبهيت الشاشة، نقطة ذيل متأخرة لكل شرارة، بدون هالات، الشرارات البطيئة `fillRect`، وميزانية بكسل ~1.1M.
- **التخطيط:** الصفحة الرئيسية بشكل Claude Design ("داقش / ولا تناقش") والقائمة مدمجة فيها مباشرة — لا صفحة ترحيب منفصلة. اللاب له تخطيط حقيقي، مو نسخة عمودية من الجوال.
- **زر واحد لكل فعل** — لا أزرار مكررة.
- سلايدر المراهنة يبقى، لا يتحول أزرار.
- `$` على الطاولة فقط (المقاعد، الفيش، القدر ← `money()`). أي رقم جنب كلام عربي أو زر بدون `$` (`fmt()`).
- الجولة الجاية تبدأ لحالها (1s، و7s بعد كشف داقش عشان الكل يلحق يشوف الورق) بدون شريط عد. وفي شاشة كشف الداقش زر «تمام، شفت» يسكّرها ويعلن إنك جاهز.

- البوتات يردّون في الدردشة بلهجات سعودية (جداوي: فيصل، منى، عبدالملك، ريم، هيا، ماجد. نجدي: الباقي)، بعد ما "يكتبون"، وعلى البشر بس.

## الحدود
لا توسّع النطاق بدون إذن. فكرة الرسائل الصوتية (اضغط-وتكلم) وفرض الصوت على الآيفون الصامت: مطروحة وما تمّت الموافقة عليها.
